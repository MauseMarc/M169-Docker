apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: casino-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: casino.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: website-service
            port:
              number: 80